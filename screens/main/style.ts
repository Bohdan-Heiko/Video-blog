import { StyleSheet } from "react-native"

export const style = StyleSheet.create({
  main: {
    flex: 1,
    paddingLeft: 16,
    paddingTop: 15
  },
  mainContainer: {
    gap: 44,
    paddingBottom: 25
  }
})
